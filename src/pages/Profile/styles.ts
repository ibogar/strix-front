import styled from 'styled-components'

export const Content = styled.div`
    width: 100%;
    display: grid;
    align-items: start;
    gap: 16px;
    grid-template-columns: 25% 75%;
    padding: 16px;
`